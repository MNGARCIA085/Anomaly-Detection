batch_size: 32
epochs: 5
type: "default" # or "vae", "transfer"

optimizer:
  name: adam
  params:
    lr: 0.001
    betas: [0.9, 0.999]

loss:
  name: mse


callbacks:
  - name: print_loss
  - name: early_stopping
    params:
      patience: 5